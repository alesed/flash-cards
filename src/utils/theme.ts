import createTheme from '@mui/material/styles/createTheme';

export const theme = createTheme({
	palette: {
		mode: 'light',
		primary: {
			main: '#283593',
			light: '#d1d9ff'
		},
		secondary: {
			main: '#7b1fa2',
			light: '#fff1ff'
		}
	}
});
